# Settings (placeholder)

Ficheiros de configuração de **runtime** (Django `settings.py`, `docker-compose`, env) **não** são canónicos neste repositório.

- Use `.env.example` nos repos de aplicação.
- Aqui apenas política: variáveis sensíveis nunca commitadas; permissão 600 no servidor.
